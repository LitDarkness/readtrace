# 读迹 ReadTrace 简短说明

读迹是一个按 Workspace 管理多个 Vault 的 OCR 文本整理工具。TXT/Markdown 直接读取，图片/PDF 使用真实 Tesseract/Poppler，文件夹递归解析支持格式，其它格式跳过。

核心流程是：

```text
import → ocr → normalize → repair → build
```

跨 batch 合并以单个 source 文件或 clean Markdown 为最小单元，先生成可人工编辑的 merge plan，再确认并生成 revision。图片/PDF 的 raw OCR 和简单 normalization 不得作为最终 build 或问答引用；必须先完成整页 repair，TXT/Markdown 则可直接使用。

LLM 以整页为单位返回完整 `repaired_text`，可以修复错字、断行、标点和角色标签，不再要求人工逐条接受 patch，也没有 confidence 分级。原素材默认复制到 `sources/`，也可以 `--no-copy` 保存外部路径；raw OCR、规范化、每页 checkpoint 和所有历史 revision 都保留，人工可以随时对照、编辑或恢复。

提示词放在 `prompts/repair.md`，角色别名和专名放在 `prompts/profile.md`，都是普通 Markdown。HTTP OpenAI-compatible Provider 可接清华网关、GLM 或其它自定义 Base URL；Codex Provider 包装本机 `codex exec`，例如 `--preset codex-luna --thinking high`。

搜索只使用本地 SQLite，不调用 LLM。每次 repair/answer 调用都会写入 `runtime/calls.jsonl`：调用次数始终记录，Token 缺失为 null，只有价格和 Token 都已知时计算 USD/CNY。课程要求的开发阶段 AI 对话成本 Excel 独立人工维护。

详细架构、CLI 和交付清单分别见 `docs/ARCHITECTURE_EXPLAINED.md`、`docs/CLI_TUTORIAL.md`、`docs/DELIVERABLES_AND_COST_NOTES.md`。
