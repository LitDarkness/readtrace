# 示例 profile：剧情对白

这是一个示例，不会自动加载。复制为某个 Vault 的 `prompts/profile.md` 后再运行 `repair`。

- `KEW`、`BRIS`、`BRS`、`BFS`、`MBA`、`SW` 等 OCR 说话人缩写，在本资料中都指旅行者 `Banished`。
- 只有上下文充分时才恢复专名；不确定的名字保留 OCR 结果，不能编造。
- 保留对白、旁白和段落结构，不要添加文本框、引用框或额外解释。
